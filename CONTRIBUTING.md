# Contributing

1. `main`から目的ごとの作業ブランチを作成してください。
2. 変更は小さく保ち、`npm run check`と`npm run build`で確認してください。
3. Pull Requestへ変更理由、影響範囲、確認結果を記載してください。
4. 大きな機能追加は、実装前にIssueまたは`docs/`内の設計メモで合意してください。

実測結果、`.env.local`、秘密情報、PC名、ユーザー名、ローカルパスはコミットしないでください。
